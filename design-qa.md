# 全站业务详情界面视觉验收

## 验收范围

- 小时趋势：数据点详情。
- 经营总览：自然小时分时详情。
- 预警中心：主播趋势事实详情。
- 权限编辑、规则配置和账号维护属于操作表单，不作为业务数据详情页处理。

## 视觉基准与证据

- 原始需求截图：`C:\windows\TEMP\codex-clipboard-6417c707-3c55-402c-b9c0-69c9596ff3a0.png`。
- 已通过阶段 34 验收的数据点详情基准：`docs/ui/evidence/after/data-point-detail-600x1200.png`。
- 本次数据点详情：`docs/ui/evidence/after/states/data-point-detail-600x1200.png`。
- 本次分时详情：`docs/ui/evidence/after/states/hourly-detail-600x1200.png`。
- 本次主播趋势详情：`docs/ui/evidence/after/states/anchor-trend-detail-600x1200.png`。
- 四图同屏对照：`docs/ui/evidence/after/all-detail-pages-comparison.png`。
- 所有主验收截图均使用 600×1200 CSS 视口、`deviceScaleFactor=1`，无浏览器外框和设备边框。

## 同屏视觉结论

- 三类详情均延续全站暖白背景、白色卡片、16px 圆角、轻阴影和橙/蓝/紫业务识别色，没有引入第二套视觉系统。
- 首屏信息层级统一为：详情标题、业务对象、状态标签、四项核心上下文、补充口径；后续统一使用英文小眉题、中文章节标题和指标卡网格。
- 分时详情把当前/基准 ROI、目标、涨幅、消耗和完整率整理为八张卡，并把跳转、预警和 CSV 导出放入独立操作条。
- 主播趋势详情把当前/基准 ROI、分子、分母、目标和变化整理为八张卡，计算口径与判断说明独立成可读结论块。
- 数据点详情继续按指标口径分组，真实采集记录和原始字段能力没有移除。
- 600px 下三类详情均为双列指标卡；更窄的手机断点继续切换为单列。表格只在自身容器滚动，不造成页面级横向溢出。

## 交互与数据边界

- 详情打开、关闭、标签切换、时间线跳转、预警跳转和 CSV 导出入口均保留原有行为。
- 三类详情继续读取既有 API、权限范围、指标字典和原始事实；本次没有新增、推导或覆盖业务数据。
- 隔离 E2E 使用匿名测试数据库和 Mock 飞书配置；只有为主播趋势详情生成测试事实时写入隔离数据库，不访问正式数据，不发送飞书消息。
- 无障碍中文标签已复核；状态不只依赖颜色，同时包含图标与文字。

## 自动验收

- React 定向组件测试：3 个文件、14 项全部通过。
- 详情抽屉 E2E：3 类详情在 600×1200 视口全部打开成功，抽屉边界和内部 `scrollWidth` 均未超出视口。
- ESLint、TypeScript 和 Prettier 已通过。
- 同屏对照未发现可执行的 P0、P1、P2 或 P3 视觉问题。

## Findings

- P0：无。
- P1：无。
- P2：无。
- P3：无。

final result: passed
