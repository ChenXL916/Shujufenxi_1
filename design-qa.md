# 阶段 37 设计 QA：曲线易点选与主播排序

## 对照基准

- 用户问题截图：`C:\windows\TEMP\codex-clipboard-17e2e0f4-5d83-4b5c-9930-a65638b58bcc.png`
  - 325 × 162 px。
  - 用于确认曲线可见点约 6–7px、笔记本触控板难以精确命中的问题。
- 主播分析改动前基准：`artifacts/stage37/source-anchors-1440x900.png`
  - 浏览器视口 1440 × 900 CSS px，`deviceScaleFactor=1`。
  - 截图像素 1440 × 1494；隔离 E2E 数据，日期 2026-07-17，默认 20 个分析指标。
- 小时趋势改动前基准：`artifacts/stage37/source-timeline-1440x900.png`
  - 浏览器视口 1440 × 900 CSS px，`deviceScaleFactor=1`。
  - 截图像素 1440 × 3610；隔离 E2E 数据，小时粒度。

## 实现证据

- 主播分析实现：`artifacts/stage37/implementation-anchors-1440x900.png`
  - 截图像素 1440 × 1482；与基准相同视口、数据、筛选和主题。
- 小时趋势实现：`artifacts/stage37/implementation-timeline-1440x900.png`
  - 截图像素 1440 × 3610；与基准相同视口、数据、筛选和主题。
- 全页并排对照：
  - `artifacts/stage37/anchors-full-before-after.png`
  - `artifacts/stage37/timeline-full-before-after.png`
- 重点区域并排对照：
  - `artifacts/stage37/anchors-focused-before-after.png`
  - `artifacts/stage37/timeline-focused-before-after.png`

## 对比结论

- 字体与排版：沿用现有中文字体栈、字号、字重和行高；时均成交与排序按钮没有改变页面标题、筛选区或表格正文层级。
- 间距与布局：排序按钮位于数值列表头名称后，单按钮 24 × 24px；表格继续在自身容器横向滚动，1440px 与 1920px E2E 截图均无页面级横向溢出。
- 颜色与视觉令牌：按钮默认使用弱文本色，悬停、聚焦和选中使用现有蓝色信息令牌；曲线默认画面与改动前像素观感一致，没有新增图例或放大原数据点。
- 图像与资产：没有新增图片、插画或自绘图标；升降序使用项目已有 Ant Design 图标。透明点击辅助系列仅扩展交互命中区域，不替代业务曲线。
- 文案与内容：新增“时均成交”，数值按货币格式显示；升序和降序按钮有完整中文 `aria-label`、`title` 和 `aria-pressed` 状态。

## 交互与数据验证

- 每个有效曲线点新增 24px 点击热区，悬停显示同系列颜色的聚焦圆环；原可见点仍为 6–7px。
- 热区点击按辅助系列 ID 映射回原业务系列和原 `dataIndex`；单测覆盖小时趋势、当前周期、对比周期和附加指标曲线。
- 主播汇总的有效小时、直播间数、时均成交和所有动态数值列均提供常显升序/降序按钮；E2E 验证时均成交升序按钮可见、可点击并进入选中状态。
- `make.cmd check` 的 Chromium 端到端测试 7/7 通过；全页主题、命名按钮、页面溢出、核心详情和导出链路均通过，未发现控制台脚本错误或失败资源请求。

## 比较历史

1. 初始问题：
   - P1：曲线视觉点命中范围过小，笔记本触控板难以打开详情。
   - P2：主播分析缺少固定时均成交，数值列没有可见的单击升降序入口。
2. 修复：
   - 增加不改变默认视觉的 24px 透明命中层和悬停聚焦圆环。
   - 增加时均成交列及所有数值列表头的常显升降序按钮。
3. 修复后证据：
   - 小时趋势并排图确认图例、线条、原数据点、字体、间距和色彩没有视觉漂移。
   - 主播分析并排图确认新列与排序控件对齐、可辨识且没有挤压主播名称或造成页面级溢出。
   - 无遗留 P0、P1 或 P2 问题。

## 最终结果

final result: passed
