# 数据点详情视觉验收

## 对比目标

- 源视觉真值：`C:\windows\TEMP\codex-clipboard-6417c707-3c55-402c-b9c0-69c9596ff3a0.png`
- 实现截图：`docs/ui/evidence/after/data-point-detail-600x1200.png`
- 移动端截图：`docs/ui/evidence/after/data-point-detail-390x844.png`
- 同屏对比：`docs/ui/evidence/after/data-point-detail-comparison.png`
- 状态：小时趋势数据点详情已打开，使用本地运行库中的真实标准化小时事实和采集点，只读浏览。

## 视口与像素归一化

- 源图像：600 × 1200 px。
- 实现图像：600 × 1200 px；CSS 视口 600 × 1200；`deviceScaleFactor=1`。
- 移动端图像：390 × 844 px；CSS 视口 390 × 844；`deviceScaleFactor=1`。
- 源图和主实现图像素尺寸一致，无缩放、设备边框或浏览器外框差异。

## 全视图对比证据

- 原界面的基础信息使用英文键名表格，指标为无分组的连续文字列表；新界面保留相同数据内容，改为直播间概览、中文业务字段、非颜色单一表达的质量状态和按口径分组的指标卡片。
- 600px 视口中抽屉边界为 `0,0,600,1200`；三组指标网格均为双列，网格 `scrollWidth` 等于 `clientWidth`。
- 390px 视口中抽屉边界为 `0,0,390,844`；三组指标网格均切换为单列，页面和抽屉 `scrollWidth` 均等于 390px。
- 46 个实际指标值逐项检查均未发生横向截断；界面未显示 `hour_slot` 或 `anchor_match_status` 等原始英文键名。

## 聚焦区域对比

未另做裁剪。600 × 1200 同屏图中的标题、状态、四项基础信息、指标分组标题、指标名称与数值均可直接辨认，重点区域不需要放大才能判断字体、间距和对齐。

## 必查视觉面

- 字体与层级：延续全站 Inter、苹方、微软雅黑与等宽数字字体；房间名、分组标题、指标名和指标值形成四级层级，数值使用 tabular numerals。
- 间距与布局：使用现有 4/8/12/16/20 间距 token、暖白卡片、16px 圆角和轻阴影；桌面双列、手机单列，无横向溢出。
- 颜色与 token：全部来自全局暖白、橙色主强调、蓝色信息、绿/黄/红状态 token；状态同时显示图标或文字，不只依赖颜色。
- 图像与资产：界面没有业务图片；图标来自项目现有 Ant Design Icons，没有手绘 SVG、占位图或低清资产。
- 文案与内容：基础字段改为中文；`complete`、`matched` 等技术状态改为“数据完整”“排班一致”；指标名称、单位和原始数值继续来自现有指标字典与详情接口。

## 交互与运行证据

- 已测试打开详情、滚动抽屉、读取指标分组和响应式切换。
- 原始字段默认折叠，仍可展开查看完整 JSON。
- 小时详情显示全部采集记录及有效/异常状态。
- 浏览器控制台错误：0。
- 非 GET 请求和非本机请求在视觉验收中均被阻断，未触发同步、导出、预警或真实飞书操作。

## Findings

- P0：无。
- P1：无。
- P2：无。
- P3：无阻塞性问题。

## Comparison history

- Pass 1：源图与 600 × 1200 实现同屏比较，未发现可执行的 P0/P1/P2 差异；本次为明确的排版优化而非像素克隆，信息结构变化符合用户目标和现有暖白设计系统。
- 移动端复核：390 × 844 自动改为单列，标题、状态、基础信息和指标值均保持完整，无需二次视觉修复。

## Implementation checklist

- [x] 中文化基础字段。
- [x] 增加直播间、日期、小时、主播、场控和质量状态层级。
- [x] 指标按时段、累计、实时和其他口径分组。
- [x] 原始字段折叠保留。
- [x] 小时采集记录完整展示。
- [x] 600px 与 390px 响应式、溢出和控制台检查。

final result: passed
